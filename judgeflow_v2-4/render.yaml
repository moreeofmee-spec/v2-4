services:
  - type: web
    name: judgeflow
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        value: judgeflow2025titkos
      - key: PYTHON_VERSION
        value: 3.11.0
